// This component is now replaced by the MainNavBar component
// We'll keep an empty file to avoid breaking imports, but this is no longer used
import React from "react";

const MobileNav = () => {
  return null;
};

export default MobileNav;
